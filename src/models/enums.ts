export enum CapsuleStatus {
  unknown, active, retired, destroyed
}

export enum CapsuleType {
  "Dragon 1.0", "Dragon 1.1", "Dragon 2.0"
}

export enum CoreStatus {
  active, inactive, unknown, expended, lost, retired
}
export enum CrewStatus {
  active, inactive, retired, unknown
}